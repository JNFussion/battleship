const enumID = {
  PJ1: "player1",
  PJ2: "player2",
};

Object.freeze(enumID);

const enumDirection = {
  HORIZONTAL: "horizontal",
  VERTICAL: "vertical",
};

Object.freeze(enumDirection);

export { enumID, enumDirection };
